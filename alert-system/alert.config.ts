export interface AlertConfig { webhookUrl?: string; email?: string; websocketUrl?: string; thresholds: { [key: string]: number; }; } export class AlertConfiguration { private config: AlertConfig; constructor(initialConfig: AlertConfig) { this.config = initialConfig; } updateConfig(newConfig: Partial<AlertConfig>) { this.config = { ...this.config, ...newConfig }; } getConfig() { return this.config; } }