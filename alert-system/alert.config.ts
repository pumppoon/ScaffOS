export interface AlertConfig { webhookUrl?: string; email?: string; websocketUrl?: string; thresholds: Record<string, number>; } export class AlertConfiguration { private config: AlertConfig; constructor(initialConfig: AlertConfig) { this.config = initialConfig; } updateConfiguration(newConfig: Partial<AlertConfig>) { this.config = { ...this.config, ...newConfig }; } getConfiguration() { return this.config; } }